#include "main.h"

/**
 * int_abs - returns the absolute value of integer
 *
 * @n: the interger input
 * Return: always 0
 */
int int_abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -i);
	}
}
